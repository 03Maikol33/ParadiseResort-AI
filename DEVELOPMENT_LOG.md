# ParadiseResort - Development Log

Questo file tiene traccia cronologica delle attività di sviluppo e implementazione per il progetto ParadiseResort.

---

### Fase 1: Configurazione Base e Scaffolding Architetturale (In Corso)
- Analisi approfondita di `guidaAI.md`, `template2.inc.php`, struttura database in `sql/` e configurazioni in `include/`.
- Creazione dei file di tracciamento Markdown (`USER_STORIES.md`, `DESIGN_DECISIONS.md`, `DEVELOPMENT_LOG.md`).
- Aggiornamento di `include/config.inc.php` per auto-rilevamento dinamico del percorso di base (`$config['base']`).
- Allestimento della struttura delle skin (`skins/customers/` e `skins/administration/`) collegandole ai template commerciali forniti (`marian-master` e `adminhmd-1.0.0`).
